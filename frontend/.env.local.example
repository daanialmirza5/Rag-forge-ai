# Base URL the browser uses to reach the backend API directly (production).
NEXT_PUBLIC_API_URL=http://localhost:8000

# Used only by next.config.ts's dev-time rewrite (server-to-server, inside
# Docker Compose this is the backend service name, e.g. http://backend:8000).
BACKEND_INTERNAL_URL=http://localhost:8000
